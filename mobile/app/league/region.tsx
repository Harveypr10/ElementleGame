/**
 * /league/region — Region Mode League Tables
 * Simple wrapper that renders the tab league screen with gameMode='region'.
 */
import LeagueScreen from '../(tabs)/league';
export default function RegionLeagueScreen() {
    return <LeagueScreen gameMode="region" />;
}
